version: '3.9'

services:
  student-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: student-app
    ports:
      - '3000:3000'
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
